# 三种 AI 模式（科研绘图 / 思维导图 / 图表制作）+ WASD 平滑移动 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** AI 从单一"科研绘图"规范扩展为三种可选模式（科研绘图 / 思维导图 / 图表制作，自动识别 + 手动覆盖），新增 curve/sector 元素类型与 applyMindMap/applyChart 声明式工具解决"死板单一、缺乏实质内容"；同时把 WASD 视口平移改为按下即动 + rAF 连续移动，消除系统按键延迟导致的卡顿。

**Architecture:** 单一 agent 按 mode 切换系统提示（`buildSystemPrompt(mode?)`：自动模式 = 公共节 + 三节规范 + 自动识别规则；手动模式 = 公共节 + 锁定一节）。新增两个纯函数布局引擎 `mindMapLayout.ts`（放射式：中心主题 + 曲线分支 + 关键词节点 + 每分支一色）与 `chartLayout.ts`（坐标轴/刻度/柱形/折线/扇形/图例，AI 只声明类型+数据），引擎直接用 `makeElement` 产出元素，`DraftCanvas.applyMindMap/applyChart` 校验后经新增的 `pushElement` 入草稿。新元素类型 curve（二次贝塞尔分支线）/sector（饼图扇形）为 AI 生成专用，全链路（类型/工厂/命中/渲染/导出/属性面板）接入。前端 ChatPanel 加四枚分段模式按钮（自动/科研绘图/思维导图/图表制作），选择按画布 id 持久化到 localStorage，随请求体带 `mode`。

**Tech Stack:** Next.js 15 / React 19 / TypeScript / Zustand 5 / Vercel AI SDK + zod / vitest + Testing Library / dagre（复用）。

**规格**：`docs/superpowers/specs/2026-08-06-三种AI模式-design.md`

---

## 执行进度（2026-08-06 会话检查点）

> 会话中断时工作区为干净 git 状态。**任务列表（TodoWrite）是会话级的，关机即丢失——本表是下次会话的唯一进度依据。** 继续执行的入口：用 superpowers:subagent-driven-development 从下表最后一个 in-progress 任务继续；两个评审已通过的任务在表中有标注，勿重复评审。

| 任务 | 状态 | 备注 |
|---|---|---|
| Task 1 WASD 平滑移动 | ✅ 完成 | 94827c0：按下即动 + rAF 33ms 步进；测试期望按 16ms 帧网格修正 |
| Task 2 curve/sector 元素类型 | ✅ 完成 | 25249a8 + e6bb36e：curve（贝塞尔）+ sector（扇形）全链路；largeArc 跨 0 扫角修复 |
| Task 3 mindMapLayout 引擎 | ✅ 完成 | a2adb57 + 8f8a734 + a30adc0：放射布局 + 超画布缩放；curvature 符号、text 对比色、newId 三处修复 |
| Task 4 applyMindMap 工具 | ✅ 完成 | a052c90 + 0be5739：声明式工具 + 递归空关键词校验 + 5 色调色板描述 |
| Task 5 chartLayout 引擎 + polyline arrow | ✅ 完成 | b4f30ed + 968e05e：柱/折线/饼/散点 + 自动刻度/图例/标签；深色描边色板 + 刻度精度修复 |
| Task 6 applyChart 工具 | ✅ 完成 | 681e932 + e8f8654：声明式图表工具 + ChartSpec 复用 + 空标签/零总和校验 |
| Task 7 三模式提示与请求流 | ✅ 完成 | 53c08cf + b2b5fd3：buildSystemPrompt 三节结构 + ChatPanel 模式按钮 + localStorage 持久化 + aria-pressed |
| Task 8 收尾（changelog/README/检查点） | ✅ 完成 | 1ab675b：README 补三种模式与 WASD、changelog 核对、偏离记录同步 |
| Task 9 选择与小手合并（追加） | ✅ 完成 | ed4cd6e + b0ae834：删除工具栏两按钮；空白左键拖动=平移、右键拖动=多选；点击清空/Shift 追加保留；pan 点击阈值注释 + shiftKey 按下时记录 |

**当前验证基线**：`npm test` = 23 文件 248 测试全绿（tsc --noEmit + vitest，2026-08-06 Task 9 收尾时点）；`next dev -p 3001` 编译通过（用户实例 200 响应）。
**已知工作区约定**：功能改动追加 `src/lib/changelog.ts` 当前版本条目；控制器在实现/修复后把偏离同步回本计划文档并单独提交。

---

## Task 1: WASD 平滑移动（按下即动 + rAF 连续移动）

**问题根因**：现有 WASD 依赖 OS 按键自动重复（keydown repeat）。Windows 首次重复延迟约 500ms，期间画面不动，用户以为没按上；随后又按系统固定间隔步进，整体"卡一下再连续动"，操作不跟手。

**方案**：keydown 立即移动一步（不等 rAF 帧），同时启动 rAF 循环，每 33ms 一步持续移动（≈30fps，与原"每次 50px"速率一致）；keyup 停止；window blur 清空按键防卡键。支持 W+D 斜向移动（自然行为）。

**Files:**
- Modify: `src/components/editor/useShortcuts.ts`
- Test: `src/components/editor/shortcuts.test.tsx`

- [ ] **Step 1: 改写 WASD 测试（先失败）**

`src/components/editor/shortcuts.test.tsx` 中把现有 5 个 WASD 相关测试替换为下面 7 个（原"WASD 平移视口"测试在新语义下失败——按下 W 后不松开再按 S 会互相抵消，必须先 keyup）：

```tsx
it("WASD 按下即动：W 上 / A 左 / S 下 / D 右（相机语义，每次 50px）", () => {
  render(<EditorHost />);
  const v0 = useCanvasStore.getState().view;
  fireEvent.keyDown(window, { key: "w" });
  expect(useCanvasStore.getState().view.oy).toBe(v0.oy + 50); // 上：内容下移
  fireEvent.keyUp(window, { key: "w" });
  fireEvent.keyDown(window, { key: "s" });
  expect(useCanvasStore.getState().view.oy).toBe(v0.oy); // 下：回到原位
  fireEvent.keyUp(window, { key: "s" });
  fireEvent.keyDown(window, { key: "a" });
  expect(useCanvasStore.getState().view.ox).toBe(v0.ox + 50); // 左
  fireEvent.keyUp(window, { key: "a" });
  fireEvent.keyDown(window, { key: "d" });
  expect(useCanvasStore.getState().view.ox).toBe(v0.ox); // 右：回到原位
  fireEvent.keyUp(window, { key: "d" });
  expect(useCanvasStore.getState().view.scale).toBe(v0.scale); // 缩放不变
});

it("按住 W 持续移动（33ms 一步），松手停止", () => {
  render(<EditorHost />);
  // 渲染后再开假定时器：React 初始渲染用真实调度，keydown 后循环才用假 rAF
  vi.useFakeTimers({
    toFake: ["setTimeout", "clearTimeout", "setInterval", "clearInterval", "Date", "requestAnimationFrame", "cancelAnimationFrame", "performance"],
  });
  try {
    const v0 = useCanvasStore.getState().view;
    fireEvent.keyDown(window, { key: "w" });
    expect(useCanvasStore.getState().view.oy).toBe(v0.oy + 50); // 按下即动
    vi.advanceTimersByTime(100);
    // rAF 帧在 t≈16/33/50/66/83/100：t-lastMove≥33 的帧（33/66/99）各 +50，共 +150
    expect(useCanvasStore.getState().view.oy).toBe(v0.oy + 200);
    fireEvent.keyUp(window, { key: "w" });
    const after = useCanvasStore.getState().view.oy;
    vi.advanceTimersByTime(200);
    expect(useCanvasStore.getState().view.oy).toBe(after); // 松手后不再移动
  } finally {
    vi.useRealTimers();
  }
});

it("同时按住 W+D 斜向移动", () => {
  render(<EditorHost />);
  const v0 = useCanvasStore.getState().view;
  fireEvent.keyDown(window, { key: "w" });
  fireEvent.keyDown(window, { key: "d" });
  expect(useCanvasStore.getState().view.ox).toBe(v0.ox - 50);
  expect(useCanvasStore.getState().view.oy).toBe(v0.oy + 50);
  fireEvent.keyUp(window, { key: "w" });
  fireEvent.keyUp(window, { key: "d" });
});

it("窗口失焦清空按键：松手前失焦不再移动", () => {
  render(<EditorHost />);
  vi.useFakeTimers({
    toFake: ["setTimeout", "clearTimeout", "setInterval", "clearInterval", "Date", "requestAnimationFrame", "cancelAnimationFrame", "performance"],
  });
  try {
    const v0 = useCanvasStore.getState().view;
    fireEvent.keyDown(window, { key: "a" });
    expect(useCanvasStore.getState().view.ox).toBe(v0.ox + 50);
    fireEvent.blur(window);
    const after = useCanvasStore.getState().view.ox;
    vi.advanceTimersByTime(200);
    expect(useCanvasStore.getState().view.ox).toBe(after); // blur 后循环停止
  } finally {
    vi.useRealTimers();
  }
});

it("WASD 平移不改变选中元素位置", () => {
  const a = makeElement("rect", 0, 0, 50, 50);
  useCanvasStore.getState().addElement(a);
  useCanvasStore.getState().setSelection([a.id]);
  render(<EditorHost />);
  fireEvent.keyDown(window, { key: "d" });
  expect(useCanvasStore.getState().doc.elements[0].x).toBe(0);
  fireEvent.keyUp(window, { key: "d" });
});

it("Ctrl+W 等修饰键组合不触发平移（保留浏览器/系统快捷键）", () => {
  render(<EditorHost />);
  const v0 = useCanvasStore.getState().view;
  fireEvent.keyDown(window, { key: "w", ctrlKey: true });
  fireEvent.keyDown(window, { key: "a", metaKey: true });
  expect(useCanvasStore.getState().view.ox).toBe(v0.ox);
  expect(useCanvasStore.getState().view.oy).toBe(v0.oy);
});

it("输入框内按 WASD 不触发平移（打字不移动画布）", () => {
  render(<EditorHost />);
  const v0 = useCanvasStore.getState().view;
  const ta = document.createElement("textarea");
  document.body.appendChild(ta);
  ta.focus();
  fireEvent.keyDown(ta, { key: "a" });
  expect(useCanvasStore.getState().view.ox).toBe(v0.ox);
  document.body.removeChild(ta);
});

it("AI 生成中 WASD 仍可平移（边看 AI 绘制边导航）", () => {
  useCanvasStore.getState().setGenerating(true);
  render(<EditorHost />);
  const v0 = useCanvasStore.getState().view;
  fireEvent.keyDown(window, { key: "d" });
  expect(useCanvasStore.getState().view.ox).toBe(v0.ox - 50);
  // 生成中 Delete 等编辑快捷键仍被忽略
  fireEvent.keyDown(window, { key: "Delete" });
  expect(useCanvasStore.getState().doc.elements).toHaveLength(0);
  fireEvent.keyUp(window, { key: "d" });
});
```

删除原 5 个 WASD 测试：`WASD 平移视口：W 上 / A 左 / S 下 / D 右`、`WASD 平移不改变选中元素位置`、`Ctrl+W 等修饰键组合不触发平移`、`输入框内按 WASD 不触发平移`、`AI 生成中 WASD 仍可平移`。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/editor/shortcuts.test.tsx`
Expected: 测试失败——旧实现 keydown 后无 keyup 监听，`fireEvent.blur` 无效果，"按住 W 持续移动"测试停在按下即动的 50px（+50 ≠ +200）。

- [ ] **Step 3: 实现平滑移动**

`src/components/editor/useShortcuts.ts` 整体替换为：

```ts
"use client";

import { useEffect } from "react";
import { useCanvasStore } from "@/lib/canvas/store";
import { newId } from "@/lib/canvas/elements";

export function useShortcuts() {
  useEffect(() => {
    // WASD 连续移动：按下即动一步 + rAF 循环每 33ms 一步（≈30fps，与原速率一致）。
    // 不依赖 OS 按键自动重复——Windows 首次重复延迟约 500ms 导致"卡一下再动"，操作不跟手
    const pressed = new Set<string>();
    let rafId: number | null = null;
    let lastMove = 0;

    const stopLoop = () => {
      if (rafId != null) {
        cancelAnimationFrame(rafId);
        rafId = null;
      }
    };
    const moveStep = () => {
      const s = useCanvasStore.getState();
      const dx = (pressed.has("a") ? 1 : 0) - (pressed.has("d") ? 1 : 0);
      const dy = (pressed.has("w") ? 1 : 0) - (pressed.has("s") ? 1 : 0);
      s.setView({ scale: s.view.scale, ox: s.view.ox + dx * 50, oy: s.view.oy + dy * 50 });
    };
    const loop = (t: number) => {
      if (t - lastMove >= 33) {
        moveStep();
        lastMove = t;
      }
      rafId = requestAnimationFrame(loop);
    };
    const startLoop = () => {
      if (rafId == null) {
        lastMove = performance.now();
        rafId = requestAnimationFrame(loop);
      }
    };

    const onKey = (e: KeyboardEvent) => {
      const s = useCanvasStore.getState();
      // 在输入框/文本框内不拦截快捷键（文字编辑时 Ctrl+Z 应恢复输入框自身的撤销）
      const t = e.target as HTMLElement | null;
      if (t && (t.tagName === "INPUT" || t.tagName === "TEXTAREA")) return;
      if (e.ctrlKey || e.metaKey) {
        const k = e.key.toLowerCase();
        if (k === "z" && !e.shiftKey) { e.preventDefault(); s.undo(); }
        else if (k === "y" || (k === "z" && e.shiftKey)) { e.preventDefault(); s.redo(); }
        else if (k === "d") { e.preventDefault(); duplicateSelection(); }
        return;
      }
      // WASD 平移视口（按下即动 50px，按住 rAF 循环每 33ms 一步）；仅移动视图，不影响元素与选区。
      // 相机语义：W=上（内容下移 oy+）、A=左、S=下、D=右；AI 生成中也可平移（边看 AI 绘制边导航）
      const k = e.key.toLowerCase();
      if (k === "w" || k === "a" || k === "s" || k === "d") {
        e.preventDefault();
        if (!pressed.has(k)) {
          pressed.add(k);
          moveStep(); // 按下即动：不等 rAF 帧，消除系统按键延迟的"卡顿感"
          startLoop();
        }
        return;
      }
      if (s.isGenerating) return;
      if (e.key === "Delete" || e.key === "Backspace") {
        if (s.editingText) return;
        if (s.selection.length) {
          e.preventDefault();
          s.deleteElements(s.selection);
        }
      }
    };
    const onKeyUp = (e: KeyboardEvent) => {
      const k = e.key.toLowerCase();
      if (k === "w" || k === "a" || k === "s" || k === "d") {
        pressed.delete(k);
        if (pressed.size === 0) stopLoop();
      }
    };
    const onBlur = () => {
      pressed.clear();
      stopLoop();
    };
    window.addEventListener("keydown", onKey);
    window.addEventListener("keyup", onKeyUp);
    window.addEventListener("blur", onBlur);
    return () => {
      window.removeEventListener("keydown", onKey);
      window.removeEventListener("keyup", onKeyUp);
      window.removeEventListener("blur", onBlur);
      stopLoop();
    };
  }, []);
}

function duplicateSelection() {
  const s = useCanvasStore.getState();
  const selected = s.doc.elements.filter((e) => s.selection.includes(e.id));
  if (!selected.length) return;
  const copies = selected.map((e) => {
    const copy = structuredClone(e);
    copy.id = newId();
    copy.x += 20;
    copy.y += 20;
    if (copy.type === "polyline") {
      copy.points = copy.points.map((p) => ({ x: p.x + 20, y: p.y + 20 }));
    }
    return copy;
  });
  s.addElements(copies);
  s.setSelection(copies.map((c) => c.id));
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/components/editor/shortcuts.test.tsx`
Expected: 全部通过。再跑 `npm test` 确认无回归（现有 212 测试全绿）。

- [ ] **Step 5: 提交**

```bash
git add src/components/editor/useShortcuts.ts src/components/editor/shortcuts.test.tsx src/lib/changelog.ts
git commit -m "feat: WASD 平滑移动——按下即动 + rAF 每 33ms 一步，消除系统按键重复延迟的卡顿
（changelog 追加见 Step 6）"
```

- [ ] **Step 6: changelog 追加**

`src/lib/changelog.ts` 顶部条目（time: "2026-08-05 23:30"）的 `changes` 数组末尾追加：

```ts
"WASD 平移更跟手：按下即动 + 按住期间 rAF 循环每 33ms 一步平滑移动（不再依赖系统按键重复，消除首次延迟约 500ms 的卡顿；支持 W+D 等斜向移动，窗口失焦自动停）",
```

若尚未提交则与 Step 5 一起提交；否则单独提交 `git commit -m "docs: changelog WASD 平滑移动"`。

---

## Task 2: curve / sector 元素类型（全链路）

新增两种 AI 生成专用元素类型（无工具栏工具、无手动编辑手柄）：

- `curve`：二次贝塞尔分支线。`x/y` = 起点，`width/height` = 终点相对偏移，`curvature` = 控制点沿起终点法线偏移系数（0=直线，正负号控制凸向，默认 0.5）。控制点 = 起终点中点 + 法线 × curvature × 线长。
- `sector`：饼图扇形。`x/y` = 圆心，`radius` + `startAngle/endAngle`（弧度，0 在 3 点钟方向顺时针）。width/height = 2r（包围盒用）。

**Files:**
- Modify: `src/lib/canvas/types.ts`
- Modify: `src/lib/canvas/elements.ts`
- Modify: `src/lib/canvas/geometry.ts`
- Modify: `src/components/editor/ElementShape.tsx`
- Modify: `src/lib/canvas/exporter.ts`
- Modify: `src/lib/ai/draft.ts`
- Modify: `src/components/editor/PropertyPanel.tsx`
- Modify: `src/components/editor/SelectionOverlay.tsx`
- Test: `src/lib/canvas/elements.test.ts`
- Test: `src/lib/canvas/exporter.test.ts`

- [ ] **Step 1: 写失败测试（类型/工厂/命中/导出）**

`src/lib/canvas/elements.test.ts` 末尾追加：

```ts
describe("curve / sector 元素类型", () => {
  it("makeElement curve：默认 curvature 0.5", () => {
    const c = makeElement("curve", 10, 20, 100, 60);
    expect(c.type).toBe("curve");
    if (c.type === "curve") {
      expect(c.curvature).toBe(0.5);
      expect(c.x).toBe(10);
      expect(c.y).toBe(20);
      expect(c.width).toBe(100);
      expect(c.height).toBe(60);
    }
  });

  it("makeElement sector：radius 与角度", () => {
    const s = makeElement("sector", 100, 100, 40, 40, { radius: 20, startAngle: 0, endAngle: Math.PI / 2 });
    expect(s.type).toBe("sector");
    if (s.type === "sector") {
      expect(s.radius).toBe(20);
      expect(s.startAngle).toBe(0);
      expect(s.endAngle).toBeCloseTo(Math.PI / 2);
    }
  });

  it("hitTestElement 命中 curve 曲线附近，远处不命中", () => {
    const c = makeElement("curve", 0, 0, 100, 0, { curvature: 0 }); // 直线段 (0,0)→(100,0)
    expect(hitTestElement(c, { x: 50, y: 6 }, 8)).toBe(true);   // 线上 6px
    expect(hitTestElement(c, { x: 50, y: 40 })).toBe(false);    // 远处
  });

  it("hitTestElement 命中 sector 扇形内，角度外/半径外不命中", () => {
    const s = makeElement("sector", 0, 0, 20, 20, { radius: 10, startAngle: 0, endAngle: Math.PI / 2 });
    expect(hitTestElement(s, { x: 7, y: 7 })).toBe(true);   // 45° 方向半径内
    expect(hitTestElement(s, { x: -7, y: 7 })).toBe(false); // 135° 方向角度外
    expect(hitTestElement(s, { x: 15, y: 15 })).toBe(false); // 半径外
  });
});
```

`src/lib/canvas/exporter.test.ts` 末尾追加（先读该文件现有结构，沿用其导入与断言风格）：

```ts
it("elementToSvg 导出 curve（二次贝塞尔 Q）", () => {
  const c = makeElement("curve", 0, 0, 100, 0, { curvature: 0, stroke: "#2f2f2f" });
  const svg = elementToSvg(c);
  expect(svg).toContain("M 0 0 Q 50 0 100 0");
});

it("elementToSvg 导出 sector（圆心+起边线+圆弧）", () => {
  const s = makeElement("sector", 100, 100, 40, 40, { radius: 20, startAngle: 0, endAngle: Math.PI / 2 });
  const svg = elementToSvg(s);
  expect(svg).toContain("M 100 100 L 120 100 A 20 20 0 0 1 100 120 Z");
});
```

注：curve 导出前确认 `curveControl` 公式与上面 M/Q 期望一致（curvature 0 → 控制点 = 中点 (50,0)）。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/lib/canvas/elements.test.ts src/lib/canvas/exporter.test.ts`
Expected: FAIL——TS 报 `curve`/`sector` 不在 ElementType 联合；`makeElement` 无对应分支；`hitTestElement` 无对应 case。

- [ ] **Step 3: types.ts 新增类型**

```ts
export type ElementType = ShapeType | "arrow" | "polyline" | "text" | "logic" | "curve" | "sector";

// 贝塞尔曲线分支线（AI 生成专用）：x/y=起点，width/height=终点相对偏移，
// 控制点 = 起终点中点沿法线偏移 curvature×线长（0=直线，正负号控制凸向）
export interface CurveElement extends BaseElement {
  type: "curve";
  curvature: number;
}

// 饼图扇形（AI 生成专用）：x/y=圆心，radius + startAngle/endAngle（弧度，0 在 3 点钟方向顺时针）
export interface SectorElement extends BaseElement {
  type: "sector";
  radius: number;
  startAngle: number;
  endAngle: number;
}

export type CanvasElement = RectElement | EllipseElement | TriangleElement | DiamondElement | HexagonElement | TextElement | ArrowElement | PolylineElement | LogicElement | CurveElement | SectorElement;
```

- [ ] **Step 4: elements.ts 工厂**

`ElementExtras` 的 `Partial<...>` 链式类型中追加 `Partial<CurveElement> & Partial<SectorElement>`（它们继承 BaseElement，合并后 `extra.curvature/radius/startAngle/endAngle` 可访问）。`makeElement` 的 switch 追加：

```ts
case "curve":
  return { ...base, type: "curve", curvature: extra.curvature ?? 0.5 } as CanvasElement;
case "sector":
  return {
    ...base,
    type: "sector",
    radius: extra.radius ?? Math.max(width, height) / 2,
    startAngle: extra.startAngle ?? 0,
    endAngle: extra.endAngle ?? Math.PI * 2,
  } as CanvasElement;
```

- [ ] **Step 5: geometry.ts 几何（控制点/包围盒/命中）**

`lineBounds` 函数中 polyline 分支后追加：

```ts
if (e.type === "curve") {
  const c = curveControl(e);
  return quadBezierBounds({ x: e.x, y: e.y }, c, { x: e.x + e.width, y: e.y + e.height });
}
if (e.type === "sector") {
  return { x: e.x - e.radius, y: e.y - e.radius, width: e.radius * 2, height: e.radius * 2 };
}
```

新增导出（放在 `shapePoints` 之前或之后均可，注意 `quadBezierBounds` 需要 `Rect` 类型）：

```ts
// 曲线控制点：起终点中点沿法线偏移 curvature×线长（法线取终点方向逆时针转 90°）
export function curveControl(e: CurveElement): Point {
  const dx = e.width;
  const dy = e.height;
  const len = Math.hypot(dx, dy) || 1;
  const px = -dy / len;
  const py = dx / len;
  return { x: e.x + e.width / 2 + px * e.curvature * len, y: e.y + e.height / 2 + py * e.curvature * len };
}

// 二次贝塞尔精确包围盒：极值点 t = (p0 - p1) / (p0 - 2p1 + p2)
export function quadBezierBounds(p0: Point, p1: Point, p2: Point): Rect {
  const xs = [p0.x, p2.x];
  const ys = [p0.y, p2.y];
  for (const [a, b, c, isX] of [
    [p0.x, p1.x, p2.x, true],
    [p0.y, p1.y, p2.y, false],
  ] as const) {
    const denom = a - 2 * b + c;
    if (denom === 0) continue;
    const t = (a - b) / denom;
    if (t > 0 && t < 1) {
      const v = (1 - t) ** 2 * a + 2 * t * (1 - t) * b + t * t * c;
      if (isX) xs.push(v);
      else ys.push(v);
    }
  }
  const minX = Math.min(...xs);
  const maxX = Math.max(...xs);
  const minY = Math.min(...ys);
  const maxY = Math.max(...ys);
  return { x: minX, y: minY, width: Math.max(maxX - minX, 1), height: Math.max(maxY - minY, 1) };
}

// 点到二次贝塞尔的近似距离（采样 16 段折线）
function distToQuadCurve(p: Point, p0: Point, c: Point, p2: Point): number {
  let best = Infinity;
  let prev = p0;
  for (let i = 1; i <= 16; i++) {
    const t = i / 16;
    const pt = {
      x: (1 - t) ** 2 * p0.x + 2 * t * (1 - t) * c.x + t * t * p2.x,
      y: (1 - t) ** 2 * p0.y + 2 * t * (1 - t) * c.y + t * t * p2.y,
    };
    best = Math.min(best, distToSegment(p, prev, pt));
    prev = pt;
  }
  return best;
}

// 角度是否落在扇形区间（弧度；处理跨 0 与整圆）
export function angleInSector(a: number, start: number, end: number): boolean {
  if (end - start >= Math.PI * 2) return true;
  const norm = (x: number) => ((x % (Math.PI * 2)) + Math.PI * 2) % (Math.PI * 2);
  const s = norm(start);
  const e = norm(end);
  const t = norm(a);
  return s <= e ? t >= s && t <= e : t >= s || t <= e;
}
```

`hitTestElement` 中 arrow/polyline 分支后追加：

```ts
if (e.type === "curve") {
  const c = curveControl(e);
  return distToQuadCurve(p, { x: e.x, y: e.y }, c, { x: e.x + e.width, y: e.y + e.height }) <= tolerance;
}
```

`hitTestElement` 的 switch 中 ellipse 分支后追加：

```ts
case "sector": {
  if (Math.hypot(p.x - e.x, p.y - e.y) > e.radius + tolerance) return false;
  return angleInSector(Math.atan2(p.y - e.y, p.x - e.x), e.startAngle, e.endAngle);
}
```

注意：文件顶部 import 类型需追加 `CurveElement`（`import type { CanvasElement, CurveElement } from "./types"`）。sector 的 pre-check 会先过 bbox 粗筛（lineBounds 已返回圆包围盒），然后进入 switch 的 sector case——但需确认 pre-check 的 `expanded` 矩形在 angle 分支前不会误杀（圆心 ± r 的方框包含所有在半径内的点 ✓）。

- [ ] **Step 6: ElementShape.tsx 渲染**

`renderBody` 的 polyline 分支后追加：

```tsx
case "curve": {
  const c = curveControl(e);
  return (
    <path
      d={`M ${e.x} ${e.y} Q ${c.x} ${c.y} ${e.x + e.width} ${e.y + e.height}`}
      fill="none"
      stroke={e.stroke}
      strokeWidth={e.strokeWidth}
      opacity={e.opacity}
    />
  );
}
case "sector": {
  const r = e.radius;
  const sx = e.x + r * Math.cos(e.startAngle);
  const sy = e.y + r * Math.sin(e.startAngle);
  const ex = e.x + r * Math.cos(e.endAngle);
  const ey = e.y + r * Math.sin(e.endAngle);
  const largeArc = e.endAngle - e.startAngle > Math.PI ? 1 : 0;
  return (
    <path d={`M ${e.x} ${e.y} L ${sx} ${sy} A ${r} ${r} 0 ${largeArc} 1 ${ex} ${ey} Z`} {...common} />
  );
}
```

curvature 为负时 Q 控制点方向自动取反，无需处理。curve/sector 不旋转（顶层 `rot` 变换照旧存在但引擎与面板不会设 rotation）。

- [ ] **Step 7: exporter.ts 导出**

`elementToSvg` 的 polyline 分支后追加：

```ts
case "curve": {
  const c = curveControl(e);
  return `<path d="M ${e.x} ${e.y} Q ${c.x} ${c.y} ${e.x + e.width} ${e.y + e.height}" fill="none" stroke="${e.stroke}" stroke-width="${e.strokeWidth}" opacity="${e.opacity}"${rot}/>`;
}
case "sector": {
  const r = e.radius;
  const sx = e.x + r * Math.cos(e.startAngle);
  const sy = e.y + r * Math.sin(e.startAngle);
  const ex = e.x + r * Math.cos(e.endAngle);
  const ey = e.y + r * Math.sin(e.endAngle);
  const largeArc = e.endAngle - e.startAngle > Math.PI ? 1 : 0;
  return `<path d="M ${e.x} ${e.y} L ${sx} ${sy} A ${r} ${r} 0 ${largeArc} 1 ${ex} ${ey} Z" ${attrs}${rot}/>`;
}
```

- [ ] **Step 8: draft.ts 支持（AI 可 updateElement 这些属性）**

`PATCH_KEYS` 追加：

```ts
const PATCH_KEYS = ["x", "y", "width", "height", "fill", "stroke", "strokeWidth", "rotation", "text", "body", "fontSize", "opacity", "bold", "italic", "align", "fontFamily", "curvature", "radius", "startAngle", "endAngle"] as const;
```

`typeName` map 追加：

```ts
curve: "曲线", sector: "扇形",
```

（curve/sector 不进 `createElement` 的 allowed 列表——它们由 applyMindMap/applyChart 引擎创建，AI 不得手绘。）

- [ ] **Step 9: PropertyPanel.tsx 适配**

1. 旋转行包一层条件（curve/sector 无旋转语义）：

```tsx
{one.type !== "curve" && one.type !== "sector" && (
  <div className="flex items-center gap-2">
    <span className="text-gray-500/90">旋转</span>
    <input type="number" aria-label="旋转" value={one.rotation} min={-360} max={360} onChange={(e) => patch({ rotation: Number(e.target.value) })} className="h-7 w-16 rounded-lg border border-white/60 bg-white/60 px-1.5 text-gray-700 shadow-sm outline-none focus:border-blue-300" />
  </div>
)}
```

2. logic 分支后追加两个分支（sector 的坐标/角度只读展示）：

```tsx
{one.type === "curve" && (
  <label className="flex items-center gap-2">
    <span className="text-gray-500/90">弯曲度</span>
    <input type="number" aria-label="弯曲度" value={one.curvature} min={-2} max={2} step={0.1} onChange={(e) => patch({ curvature: Number(e.target.value) } as Partial<CanvasElement>)} className="h-7 w-16 rounded-lg border border-white/60 bg-white/60 px-1.5 text-gray-700 shadow-sm outline-none focus:border-blue-300" />
  </label>
)}
{one.type === "sector" && (
  <div className="space-y-1 text-xs text-gray-500/90">
    <div>圆心 ({Math.round(one.x)}, {Math.round(one.y)})　半径 {Math.round(one.radius)}</div>
    <div>角度 {Math.round((one.startAngle * 180) / Math.PI)}° → {Math.round((one.endAngle * 180) / Math.PI)}°</div>
  </div>
)}
```

- [ ] **Step 10: SelectionOverlay.tsx 适配**

`boundsOf` 中 polyline 分支后追加（curve/sector 的选中框位置正确）：

```ts
if (e.type === "curve") {
  const c = curveControl(e);
  const xs = [e.x, c.x, e.x + e.width];
  const ys = [e.y, c.y, e.y + e.height];
  const minX = Math.min(...xs), maxX = Math.max(...xs), minY = Math.min(...ys), maxY = Math.max(...ys);
  return { x: minX, y: minY, width: Math.max(maxX - minX, 1), height: Math.max(maxY - minY, 1) };
}
if (e.type === "sector") {
  return { x: e.x - e.radius, y: e.y - e.radius, width: e.radius * 2, height: e.radius * 2 };
}
```

手柄渲染处：curve/sector 不渲染 8 向缩放手柄与旋转手柄（AI 专用、无手动编辑），只保留选中虚线框。修改 `HANDLES.map(...)` 外层为 `{e.type !== "curve" && e.type !== "sector" && (<>...手柄...</>)}`，旋转手柄同样包条件。文件顶部 import `curveControl`。

- [ ] **Step 11: 运行确认通过**

Run: `npx vitest run src/lib/canvas/elements.test.ts src/lib/canvas/exporter.test.ts src/components/editor/panels.test.tsx`
Expected: 新增测试全过 + 面板/选中框相关回归通过。再 `npm test` 全量。

- [ ] **Step 12: 提交**

```bash
git add src/lib/canvas/types.ts src/lib/canvas/elements.ts src/lib/canvas/geometry.ts src/components/editor/ElementShape.tsx src/lib/canvas/exporter.ts src/lib/ai/draft.ts src/components/editor/PropertyPanel.tsx src/components/editor/SelectionOverlay.tsx src/lib/canvas/elements.test.ts src/lib/canvas/exporter.test.ts src/lib/changelog.ts
git commit -m "feat: 新增 curve（贝塞尔分支线）/ sector（饼图扇形）元素类型——类型/工厂/命中/渲染/导出/属性面板全链路"
```

- [ ] **Step 13: changelog 追加**

```ts
"新增元素类型：curve（二次贝塞尔曲线，思维导图分支线用）与 sector（扇形，饼图用）——AI 生成专用，全链路支持（渲染/导出/命中/属性面板）",
```

---

## Task 3: mindMapLayout 放射布局引擎（纯函数）

**算法**：中心主题 logic（fontSize 18 加粗）放 (800, 470)；N 个一级分支均分 360°（从 12 点顺时针，角度 = -π/2 + i×2π/N），一级关键词 = 小 logic 节点（fontSize 13）中心在半径 300 处；子分支按父角度 ±12° 均分（spread = min(0.42, 2π/N×0.4)，偏移 = (j-(n-1)/2)×spread），半径逐层 +190；子分支关键词 = text 元素（fontSize 13）放在曲线上。分支曲线 = curve（起点 = 父节点朝向子节点的边缘锚点，终点 = 子节点中心，curvature 凸向远离画布中心一侧）。每分支一色（5 色循环，topic 白），子分支继承父分支色。整体超出画布自动缩放（复用 graphLayout 思路：位置×scale + 20 边距，节点尺寸不变）。

**Files:**
- Create: `src/lib/canvas/mindMapLayout.ts`
- Create: `src/lib/canvas/mindMapLayout.test.ts`

- [ ] **Step 1: 写失败测试**

`src/lib/canvas/mindMapLayout.test.ts`：

```ts
import { describe, it, expect } from "vitest";
import { layoutMindMap } from "./mindMapLayout";
import type { CanvasElement } from "./types";

describe("mindMapLayout", () => {
  it("3 个一级分支均分 360°（12 点起），中心主题居中偏上", () => {
    const els = layoutMindMap({ topic: "深度学习", branches: [{ keyword: "A" }, { keyword: "B" }, { keyword: "C" }] });
    const topic = els.find((e) => e.type === "logic" && e.text === "深度学习")!;
    expect(topic.x + topic.width / 2).toBeCloseTo(800);
    expect(topic.y + topic.height / 2).toBeCloseTo(470);
    const nodes = els.filter((e) => e.type === "logic" && e.text !== "深度学习");
    expect(nodes).toHaveLength(3);
    const centers = nodes.map((n) => ({ x: n.x + n.width / 2, y: n.y + n.height / 2 }));
    // 分支 0 正上方（-90°），分支 1/2 依次顺时针 120°
    expect(centers[0].y).toBeLessThan(470);
    expect(centers[1].x).toBeGreaterThan(800);
    expect(centers[1].y).toBeGreaterThan(470);
    expect(centers[2].x).toBeLessThan(800);
    // 三条分支曲线
    expect(els.filter((e) => e.type === "curve")).toHaveLength(3);
  });

  it("子分支逐层外扩、绕父角度均分，颜色继承父分支", () => {
    const els = layoutMindMap({
      topic: "T",
      branches: [{ keyword: "A", children: [{ keyword: "A1" }, { keyword: "A2" }] }],
    });
    const a = els.find((e) => e.type === "logic" && e.text === "A")!;
    const a1 = els.find((e) => e.type === "text" && e.text === "A1")!;
    const a2 = els.find((e) => e.type === "text" && e.text === "A2")!;
    // 子分支在父节点更远处（半径 +190）
    const dist = (e: CanvasElement) => Math.hypot(e.x + e.width / 2 - 800, e.y + e.height / 2 - 470);
    expect(dist(a1)).toBeGreaterThan(dist(a));
    expect(dist(a2)).toBeGreaterThan(dist(a));
    // 两个子分支在父角度两侧（一个更靠上/逆时针，一个更靠下/顺时针）
    const ang = (e: CanvasElement) => Math.atan2(e.y + e.height / 2 - 470, e.x + e.width / 2 - 800);
    expect(ang(a1)).toBeLessThan(ang(a));
    expect(ang(a2)).toBeGreaterThan(ang(a));
    // 颜色继承：A1/A2 与 A 同填充色
    expect(a1.fill).toBe(a.fill);
    expect(a2.fill).toBe(a.fill);
    // 曲线：主题→A、A→A1、A→A2 共 3 条
    expect(els.filter((e) => e.type === "curve")).toHaveLength(3);
  });

  it("每个一级分支颜色不同（5 色调色板循环）", () => {
    const els = layoutMindMap({ topic: "T", branches: [{ keyword: "a" }, { keyword: "b" }, { keyword: "c" }] });
    const fills = els.filter((e) => e.type === "logic" && e.text !== "T").map((e) => e.fill);
    expect(new Set(fills).size).toBe(3);
  });

  it("整体超出画布时自动缩放回画布内", () => {
    // 3 层深分支 + 主题：半径 300+190+190=680，45° 方向必然超界，触发缩放
    const els = layoutMindMap({
      topic: "T",
      branches: [{ keyword: "a", children: [{ keyword: "a1", children: [{ keyword: "a2" }] }] }],
    });
    for (const e of els) {
      if (e.type === "logic" || e.type === "text") {
        expect(e.x).toBeGreaterThanOrEqual(0);
        expect(e.y).toBeGreaterThanOrEqual(0);
        expect(e.x + e.width).toBeLessThanOrEqual(1600);
        expect(e.y + e.height).toBeLessThanOrEqual(1000);
      }
    }
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/lib/canvas/mindMapLayout.test.ts`
Expected: FAIL——模块不存在。

- [ ] **Step 3: 实现引擎**

`src/lib/canvas/mindMapLayout.ts`：

```ts
import { makeElement } from "./elements";
import { CANVAS_WIDTH, CANVAS_HEIGHT, curveControl, quadBezierBounds, type Point } from "./geometry";
import type { CanvasElement } from "./types";

export interface MindMapBranch {
  keyword: string;
  body?: string;
  fill?: string;
  children?: MindMapBranch[];
}
export interface MindMapSpec {
  topic: string;
  topicBody?: string;
  branches: MindMapBranch[];
}

export const MINDMAP_PALETTE = ["#eef4ff", "#f0fff0", "#fff8e6", "#f3efff", "#ffeef0"];

const TOPIC_FONT = 18;
const NODE_FONT = 13;
const R1 = 300;      // 一级分支半径
const R_STEP = 190;  // 每层递增半径
const CHILD_SPREAD_MAX = 0.42; // 子分支最大角距（±12°）

// 语义与布局分离：AI 只声明主题与分支层级，本模块做放射布局并产出元素列表
// （中心主题 + 一级关键词逻辑节点 + 子关键词 text + 分支曲线，每分支一色）。
export function layoutMindMap(spec: MindMapSpec): CanvasElement[] {
  const cx = 800;
  const cy = 470;
  const n = spec.branches.length;
  const out: CanvasElement[] = [];
  const idCounter = { i: 0 };
  const uid = (prefix: string) => `${prefix}-${idCounter.i++}`;

  // 主题节点（先算尺寸用于锚点与曲线起点）
  const topicEl = makeElement("logic", 0, 0, 0, 0, {
    text: spec.topic,
    body: spec.topicBody,
    fontSize: TOPIC_FONT,
    bold: true,
    fill: "#ffffff",
  }) as Extract<CanvasElement, { type: "logic" }>;
  topicEl.x = cx - topicEl.width / 2;
  topicEl.y = cy - topicEl.height / 2;
  topicEl.id = uid("topic");
  topicEl.zIndex = 2;

  // 一级分支：角度均分 360°
  const branchAngle = (i: number) => -Math.PI / 2 + (i * 2 * Math.PI) / n;

  interface Placed {
    el: CanvasElement;
    angle: number;
    radius: number;
    color: string;
  }
  const placed: Placed[] = [];
  const textEls: CanvasElement[] = [];

  const placeBranch = (b: MindMapBranch, angle: number, radius: number, color: string, level: number, parentCenter: Point) => {
    const center: Point = { x: cx + radius * Math.cos(angle), y: cy + radius * Math.sin(angle) };
    let el: CanvasElement;
    if (level === 1) {
      el = makeElement("logic", 0, 0, 0, 0, { text: b.keyword, body: b.body, fontSize: NODE_FONT, fill: color }) as Extract<CanvasElement, { type: "logic" }>;
    } else {
      el = makeElement("text", 0, 0, 0, 0, { text: b.keyword, fontSize: NODE_FONT, fill: "#2f2f2f" }) as Extract<CanvasElement, { type: "text" }>;
    }
    el.id = uid("node");
    el.x = center.x - el.width / 2;
    el.y = center.y - el.height / 2;
    el.zIndex = level === 1 ? 2 : 3;
    placed.push({ el, angle, radius, color });
    if (level > 1) textEls.push(el);

    // 父→子曲线：起点 = 父元素朝向子中心的边缘/锚点，终点 = 子中心
    const start = edgePointToward(parentCenter, center);
    out.push(makeCurve(start, center, color));

    const children = b.children ?? [];
    if (children.length) {
      const spread = Math.min(CHILD_SPREAD_MAX, (2 * Math.PI) / n * 0.4);
      children.forEach((child, j) => {
        const childAngle = angle + (j - (children.length - 1) / 2) * spread;
        placeBranch(child, childAngle, radius + R_STEP, color, level + 1, center);
      });
    }
  };

  spec.branches.forEach((b, i) => {
    placeBranch(b, branchAngle(i), R1, b.fill ?? MINDMAP_PALETTE[i % MINDMAP_PALETTE.length], 1, {
      x: cx,
      y: cy,
    });
  });

  // 主题→一级分支的曲线：起点 = 主题朝向分支中心的边缘点
  for (const p of placed) {
    if (p.radius === R1) {
      out.push(makeCurve(edgePointToward({ x: cx, y: cy }, centerOf(p.el)), centerOf(p.el), p.color));
    }
  }

  const all = [topicEl, ...placed.map((p) => p.el), ...out];
  return fitToCanvas(all);

  function centerOf(e: CanvasElement): Point {
    return { x: e.x + e.width / 2, y: e.y + e.height / 2 };
  }
}

// 父元素朝向目标点的边缘锚点：logic 用 4 锚点中最近的一个；text 用包围盒边缘交点（简化：最近边中点按方向取）
function edgePointToward(parent: Point, to: Point): Point {
  // parent 为父元素中心；简化处理：从父中心出发朝 to 方向，返回离父中心 8px 处的点（曲线起点）
  const d = Math.hypot(to.x - parent.x, to.y - parent.y) || 1;
  return { x: parent.x + ((to.x - parent.x) / d) * 8, y: parent.y + ((to.y - parent.y) / d) * 8 };
}

function makeCurve(start: Point, end: Point, color: string): CanvasElement {
  // 控制点凸向远离画布中心一侧
  const dx = end.x - start.x;
  const dy = end.y - start.y;
  const len = Math.hypot(dx, dy) || 1;
  const px = -dy / len;
  const py = dx / len;
  const mid = { x: (start.x + end.x) / 2, y: (start.y + end.y) / 2 };
  const side = (800 - mid.x) * px + (470 - mid.y) * py;
  const curvature = (side >= 0 ? 1 : -1) * 0.4;
  const el = makeElement("curve", start.x, start.y, end.x - start.x, end.y - start.y, {
    curvature,
    stroke: color === "#ffffff" ? "#2f2f2f" : color,
    strokeWidth: 2.5,
    zIndex: 1,
  });
  el.id = `curve-${Math.random().toString(36).slice(2, 8)}`;
  return el;
}

// 整体缩放回画布：位置缩放、节点尺寸不变（与 graphLayout 同一思路），保留 20px 边距
function fitToCanvas(els: CanvasElement[]): CanvasElement[] {
  let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
  for (const e of els) {
    let b: { x: number; y: number; width: number; height: number };
    if (e.type === "curve") {
      const c = curveControl(e);
      b = quadBezierBounds({ x: e.x, y: e.y }, c, { x: e.x + e.width, y: e.y + e.height });
    } else {
      b = { x: e.x, y: e.y, width: e.width, height: e.height };
    }
    minX = Math.min(minX, b.x);
    minY = Math.min(minY, b.y);
    maxX = Math.max(maxX, b.x + b.width);
    maxY = Math.max(maxY, b.y + b.height);
  }
  const bw = Math.max(maxX - minX, 1);
  const bh = Math.max(maxY - minY, 1);
  const scale = Math.min(1, (CANVAS_WIDTH - 40) / bw, (CANVAS_HEIGHT - 40) / bh);
  if (scale >= 1) return els;
  return els.map((e) => {
    const next = { ...e } as CanvasElement;
    next.x = (e.x - minX) * scale + 20;
    next.y = (e.y - minY) * scale + 20;
    if (e.type === "curve") {
      next.width = e.width * scale;
      next.height = e.height * scale;
    }
    return next;
  });
}
```

说明与调整点（实现时以测试为准）：
- 主题→一级分支曲线与父→子曲线分开生成，保证顺序（曲线先于节点 push，节点盖在曲线上）。
- `edgePointToward` 用"父中心朝子方向 8px"近似边缘点——主题/一级节点曲线起点视觉上贴住节点边缘；主题与一级分支的锚点精确对接不在本任务范围（后续可升级为 anchorToward，但 YAGNI）。
- 若测试断言"两个子分支在父角度两侧"失败，检查 spread 公式：2 个子分支 j=0→-0.5×spread、j=1→+0.5×spread，spread=min(0.42, 2π/1×0.4=2.51)=0.42 ✓（父分支在 -90°，a1 在上、a2 在下，ang(a1)<ang(a) ✓）。注意父分支角度为 -90° 时 atan2 返回 -π/2，a1 更靠上即更负 ✓。
- `makeCurve` 中 `stroke` 对白色分支用深色（主题色白 → 曲线深灰）保持可见。

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/lib/canvas/mindMapLayout.test.ts`
Expected: 4 测试全过（若边界断言差 1px，把 toBeCloseTo 放宽或调整 R1/R_STEP）。

- [ ] **Step 5: 提交**

```bash
git add src/lib/canvas/mindMapLayout.ts src/lib/canvas/mindMapLayout.test.ts
git commit -m "feat: mindMapLayout 放射布局引擎——中心主题 + 曲线分支 + 关键词节点 + 每分支一色 + 超画布缩放"
```

---

## Task 4: applyMindMap 工具（draft + tools + 测试）

**Files:**
- Modify: `src/lib/ai/draft.ts`
- Modify: `src/lib/ai/tools.ts`
- Test: `src/lib/ai/agent.test.ts`

- [ ] **Step 1: 写失败测试**

`src/lib/ai/agent.test.ts` 的 `describe("DraftCanvas")` 末尾追加：

```ts
it("applyMindMap 生成中心主题 + 一级分支 + 曲线 + 子分支关键词", () => {
  const d = new DraftCanvas([]);
  const r = d.applyMindMap({
    topic: "深度学习",
    branches: [
      { keyword: "数据处理", children: [{ keyword: "归一化" }] },
      { keyword: "模型" },
    ],
  });
  expect(r.ok).toBe(true);
  const els = d.serialize().elements;
  const logics = els.filter((e) => e.type === "logic");
  expect(logics).toHaveLength(3); // 主题 + 2 个一级分支
  const topic = logics.find((l) => l.text === "深度学习")!;
  expect(topic.fontSize).toBe(18);
  expect(topic.bold).toBe(true);
  expect(els.filter((e) => e.type === "curve")).toHaveLength(3); // 主题→2 分支 + 数据→归一化
  expect(els.filter((e) => e.type === "text")).toHaveLength(1);  // 子分支关键词
  expect(d.flushActivity().join("")).toContain("思维导图");
});

it("applyMindMap 校验：空主题 / 空分支 / 分支过多报错且不创建元素", () => {
  const d = new DraftCanvas([]);
  expect(d.applyMindMap({ topic: "  ", branches: [] }).ok).toBe(false);
  expect(d.applyMindMap({ topic: "T", branches: [] }).ok).toBe(false);
  expect(d.applyMindMap({ topic: "T", branches: Array.from({ length: 9 }, (_, i) => ({ keyword: `b${i}` })) }).ok).toBe(false);
  expect(d.serialize().elements).toHaveLength(0);
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/lib/ai/agent.test.ts`
Expected: FAIL——`applyMindMap` 不存在。

- [ ] **Step 3: draft.ts 实现**

新增 `pushElement` 私有方法（引擎产物绕过 createElement 的钳制与最小尺寸——connectElements 已有此先例）：

```ts
// 布局引擎产物直接入草稿（引擎坐标已规划且可能为负偏移，如向上的坐标轴箭头），不钳制
private pushElement(el: CanvasElement) {
  this.elements.push(el);
  this.ensureTextOnTop();
  this.changed();
}
```

`import { layoutMindMap } from "@/lib/canvas/mindMapLayout";` 后，在 `applyGraph` 方法后新增：

```ts
// 声明式思维导图：AI 只声明主题与分支层级，放射布局引擎产出元素（中心主题 + 曲线分支 + 关键词）
applyMindMap(args: { topic: string; topicBody?: string; branches: { keyword: string; body?: string; fill?: string; children?: { keyword: string; body?: string; children?: unknown[] }[] }[] }): { ok: boolean; error?: string } {
  const topic = (args.topic ?? "").trim();
  if (!topic) return { ok: false, error: "主题不能为空" };
  if (!args.branches || args.branches.length === 0) return { ok: false, error: "至少需要一个一级分支" };
  if (args.branches.length > 8) return { ok: false, error: "一级分支过多（最多 8 个）" };
  const els = layoutMindMap({
    topic,
    topicBody: args.topicBody,
    branches: args.branches as unknown as MindMapBranch[],
  });
  for (const el of els) this.pushElement(el);
  const nodes = els.filter((e) => e.type === "logic" || e.type === "text").length;
  const curves = els.filter((e) => e.type === "curve").length;
  this.activity.push(`思维导图已生成：主题「${topic}」+ ${args.branches.length} 个一级分支（${nodes} 个关键词、${curves} 条分支线）`);
  return { ok: true };
}
```

`import type { MindMapBranch } from "@/lib/canvas/mindMapLayout";` 或把参数类型直接展开为 `MindMapBranch[]` 并 import 类型（推荐后者，避免 any 桥）。

- [ ] **Step 4: tools.ts 注册**

`buildTools` 返回对象中 `applyGraph` 后新增：

```ts
applyMindMap: tool({
  description:
    "声明式一键生成思维导图：只需声明中心主题与分支层级（关键词/要点/子分支），系统自动完成放射布局——中心主题 + 曲线分支 + 关键词节点 + 每分支一色。" +
    "思维导图一律优先用它。关键词必须是实义语义名词（≤8 字，如“数据预处理”“损失函数”），禁止“分支1”“子项2”等空泛词；" +
    "整图 3~5 个一级分支、每分支 1~3 层子分支；分支要点用 body 展开（每行一个要点 ≤12 字）。",
  inputSchema: z.object({
    topic: z.string().describe("中心主题（简洁语义名词，≤8 字）"),
    topicBody: z.string().optional().describe("主题副标题/要点（多行用 \\n 分隔，可省略）"),
    branches: z.array(
      z.lazy(() =>
        z.object({
          keyword: z.string().describe("分支关键词（实义语义名词，≤8 字）"),
          body: z.string().optional().describe("分支要点（多行用 \\n 分隔，每行一个要点 ≤12 字，可省略）"),
          fill: z.string().optional().describe("填充色（6 色调色板：蓝/绿/橙/紫/红/白，可省略自动配色，同分支同色系）"),
          children: z.array(z.lazy(() => branchesSchema())).optional().describe("子分支（≤3 层）"),
        })
      )
    ).describe("一级分支（3~5 个）"),
  }),
  execute: (args) => draft.applyMindMap(args),
}),
```

注：`z.lazy(() => ...)` 中引用自身需要命名辅助函数（如上面 `branchesSchema`），zod 写法：

```ts
const branchSchema = z.lazy(() =>
  z.object({
    keyword: z.string(),
    body: z.string().optional(),
    fill: z.string().optional(),
    children: z.array(branchSchema).optional(),
  })
);
const branchList = z.array(branchSchema);
```

在 `buildTools` 内部（或模块级）定义后引用。TS 严格模式下 `z.lazy` 需要显式类型参数：`z.lazy<z.ZodType<...>>(() => ...)`，实现时若 TS 报循环引用，改用：

```ts
type MindMapBranchInput = { keyword: string; body?: string; fill?: string; children?: MindMapBranchInput[] };
const branchSchema: z.ZodType<MindMapBranchInput> = z.lazy(() =>
  z.object({
    keyword: z.string(),
    body: z.string().optional(),
    fill: z.string().optional(),
    children: z.array(branchSchema).optional(),
  })
);
```

`execute: (args) => draft.applyMindMap(args)`——args 类型需与 draft.applyMindMap 参数兼容（ts 类型转换 `as unknown as` 兜底）。

- [ ] **Step 5: 运行确认通过**

Run: `npx vitest run src/lib/ai/agent.test.ts`
Expected: 新增 2 测试全过；`npm test` 全绿。

- [ ] **Step 6: 提交 + changelog**

```bash
git add src/lib/ai/draft.ts src/lib/ai/tools.ts src/lib/ai/agent.test.ts src/lib/changelog.ts
git commit -m "feat: applyMindMap 声明式思维导图工具——AI 只声明主题与分支层级，放射布局引擎自动成图"
```

changelog 追加：

```ts
"思维导图模式：新增 applyMindMap 声明式工具（AI 声明主题与分支层级，系统自动放射布局：中心主题 + 曲线分支 + 关键词节点 + 每分支一色，超画布自动缩放）；关键词/要点内容规范注入提示词，杜绝空泛无实质内容",
```

---

## Task 5: chartLayout 图表引擎（纯函数）+ polyline arrow 标志

**算法**：绘图区 left=150 / right=1540 / top=130 / bottom=850。y 刻度用 niceScale（5 档候选 {1,2,2.5,5,10}×10^k 取首个 ≥ max/5，max = ceil(max/step)×step）。bar = rect（柱宽 ≤70，多系列分组并排）；line = polyline（`arrow:false`）+ 数据点椭圆（白底彩描边）；pie = sector 数组 + 百分比标签（半径 0.62 处）+ 右侧图例；scatter = 椭圆点。坐标轴 = 2 条 arrow（y 轴向上 = 负高度，走 pushElement 不经 createElement 钳制）。标题顶部居中、x/y 轴名、刻度值/类别标签、数据标签、图例色块全链齐全。系列色 = CHART_PALETTE。

**polyline arrow 标志**：`PolylineElement` 增加可选 `arrow?: boolean`（默认 true 画箭头）；ElementShape 与 exporter 在 `arrow === false` 时跳过箭头多边形。图表折线用它。

**Files:**
- Create: `src/lib/canvas/chartLayout.ts`
- Create: `src/lib/canvas/chartLayout.test.ts`
- Modify: `src/lib/canvas/types.ts`（PolylineElement.arrow?）
- Modify: `src/components/editor/ElementShape.tsx`（polyline 箭头条件）
- Modify: `src/lib/canvas/exporter.ts`（polyline 箭头条件）

- [ ] **Step 1: 写失败测试**

`src/lib/canvas/chartLayout.test.ts`：

```ts
import { describe, it, expect } from "vitest";
import { niceScale, layoutChart } from "./chartLayout";

describe("niceScale", () => {
  it("123 → 步长 25 / 上限 125（目标 5 格取整）", () => {
    expect(niceScale(123)).toEqual({ step: 25, max: 125 });
  });
  it("250 → 步长 50 / 上限 250", () => {
    expect(niceScale(250)).toEqual({ step: 50, max: 250 });
  });
  it("1 → 步长 0.2 / 上限 1", () => {
    expect(niceScale(1)).toEqual({ step: 0.2, max: 1 });
  });
});

describe("layoutChart", () => {
  it("柱状图：两条坐标轴 + 每项一根柱 + 数值标签 + 标题", () => {
    const els = layoutChart({ type: "bar", title: "季度销售额", data: [{ label: "Q1", value: 120 }, { label: "Q2", value: 80 }] });
    expect(els.filter((e) => e.type === "arrow")).toHaveLength(2);
    expect(els.filter((e) => e.type === "rect")).toHaveLength(2); // 两根柱
    expect(els.filter((e) => e.type === "text").some((t) => t.text === "季度销售额")).toBe(true);
    expect(els.filter((e) => e.type === "text").some((t) => t.text === "120")).toBe(true);
  });

  it("折线图：polyline 不带箭头，数据点椭圆，多系列图例", () => {
    const els = layoutChart({
      type: "line",
      data: [
        { label: "A", value: 1, series: "x" },
        { label: "A", value: 2, series: "y" },
        { label: "B", value: 3, series: "x" },
        { label: "B", value: 4, series: "y" },
      ],
    });
    const lines = els.filter((e) => e.type === "polyline");
    expect(lines).toHaveLength(2);
    expect(lines.every((l) => l.arrow === false)).toBe(true);
    expect(els.filter((e) => e.type === "ellipse")).toHaveLength(4); // 数据点
    expect(els.filter((e) => e.type === "rect")).toHaveLength(2);    // 图例色块
  });

  it("饼图：扇形数 = 数据项数，百分比标签，右侧图例", () => {
    const els = layoutChart({ type: "pie", data: [{ label: "A", value: 3 }, { label: "B", value: 1 }] });
    expect(els.filter((e) => e.type === "sector")).toHaveLength(2);
    expect(els.filter((e) => e.type === "text").some((t) => t.text === "75%")).toBe(true);
  });

  it("散点图：每个数据点一个椭圆，无折线", () => {
    const els = layoutChart({ type: "scatter", data: [{ label: "a", value: 5 }, { label: "b", value: 9 }] });
    expect(els.filter((e) => e.type === "ellipse")).toHaveLength(2);
    expect(els.filter((e) => e.type === "polyline")).toHaveLength(0);
  });
});
```

`src/lib/canvas/exporter.test.ts` 追加：

```ts
it("elementToSvg polyline arrow:false 不输出箭头多边形", () => {
  const p = makeElement("polyline", 0, 0, 0, 0, { points: [{ x: 0, y: 0 }, { x: 100, y: 50 }], arrow: false });
  const svg = elementToSvg(p);
  expect(svg).toContain("<polyline");
  expect(svg).not.toContain("<polygon");
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/lib/canvas/chartLayout.test.ts src/lib/canvas/exporter.test.ts`
Expected: FAIL——模块不存在、PolylineElement 无 arrow 字段（exporter 测试编译失败）。

- [ ] **Step 3: types.ts + 渲染 + 导出支持 arrow 标志**

types.ts：

```ts
export interface PolylineElement extends BaseElement {
  type: "polyline";
  points: { x: number; y: number }[];
  arrow?: boolean; // 是否画端点箭头（默认 true；图表折线用 false）
}
```

ElementShape.tsx polyline 分支：

```tsx
case "polyline": {
  const last = e.points[e.points.length - 1];
  const prev = e.points[e.points.length - 2] ?? e.points[0];
  return (
    <g>
      <polyline points={pointsToString(e.points)} fill="none" stroke={e.stroke} strokeWidth={e.strokeWidth} opacity={e.opacity} />
      {e.arrow !== false && (
        <polygon points={pointsToString(arrowHeadPoints(prev.x, prev.y, last.x, last.y))} fill={e.stroke} opacity={e.opacity} />
      )}
    </g>
  );
}
```

exporter.ts polyline 分支：

```ts
case "polyline": {
  const pts = e.points.map((p) => `${p.x},${p.y}`).join(" ");
  const head = e.arrow === false
    ? ""
    : (() => {
        const last = e.points[e.points.length - 1];
        const prev = e.points[e.points.length - 2] ?? e.points[0];
        const h = arrowHeadPoints(prev.x, prev.y, last.x, last.y).map((p) => `${p.x},${p.y}`).join(" ");
        return `<polygon points="${h}" fill="${e.stroke}" opacity="${e.opacity}"/>`;
      })();
  return `<g${rot}><polyline points="${pts}" fill="none" stroke="${e.stroke}" stroke-width="${e.strokeWidth}" opacity="${e.opacity}"/>${head}</g>`;
}
```

- [ ] **Step 4: 实现 chartLayout.ts**

```ts
import { makeElement, estimateTextSize } from "./elements";
import { CANVAS_WIDTH, CANVAS_HEIGHT } from "./geometry";
import type { CanvasElement } from "./types";

export interface ChartDatum {
  label: string;
  value: number;
  series?: string;
}
export interface ChartSpec {
  type: "bar" | "line" | "pie" | "scatter";
  title?: string;
  xLabel?: string;
  yLabel?: string;
  data: ChartDatum[];
}

export const CHART_PALETTE = ["#eef4ff", "#f0fff0", "#fff8e6", "#f3efff", "#ffeef0", "#ffffff"];
const AXIS = "#2f2f2f";
const LABEL = "#4a5568";

const PLOT = { left: 150, right: CANVAS_WIDTH - 60, top: 130, bottom: CANVAS_HEIGHT - 150 };

// 刻度取整：5 档候选 {1,2,2.5,5,10}×10^k 取首个 ≥ max/5，上限 = ceil(max/step)×step
export function niceScale(maxV: number): { step: number; max: number } {
  const raw = Math.max(maxV, 1);
  const rough = raw / 5;
  const pow = Math.pow(10, Math.floor(Math.log10(rough)));
  const step = [1, 2, 2.5, 5, 10].map((n) => n * pow).find((c) => c >= rough)!;
  return { step, max: Math.ceil(raw / step) * step };
}

// 语义与布局分离：AI 只声明图表类型与数据，本模块产出全部元素（轴/刻度/图形/标签/图例）
export function layoutChart(spec: ChartSpec): CanvasElement[] {
  if (spec.type === "pie") return pie(spec);
  return cartesian(spec);
}

function textEl(text: string, fontSize: number, x: number, y: number, opts: { bold?: boolean; fill?: string; align?: "left" | "center" | "right"; zIndex?: number } = {}): CanvasElement {
  const size = estimateTextSize(text, fontSize, opts.bold ?? false);
  const align = opts.align ?? "center";
  const tx = align === "left" ? x : align === "right" ? x - size.width : x - size.width / 2;
  return makeElement("text", tx, y - size.height / 2, size.width, size.height, {
    text,
    fontSize,
    bold: opts.bold ?? false,
    fill: opts.fill ?? LABEL,
    align,
    zIndex: opts.zIndex ?? 3,
  });
}

function cartesian(spec: ChartSpec): CanvasElement[] {
  const out: CanvasElement[] = [];
  const plotW = PLOT.right - PLOT.left;
  const plotH = PLOT.bottom - PLOT.top;
  const maxV = Math.max(...spec.data.map((d) => d.value), 1);
  const { step, max } = niceScale(maxV);
  const y = (v: number) => PLOT.bottom - (v / max) * plotH;

  // 标题 / 轴名
  if (spec.title) out.push(textEl(spec.title, 22, 800, 52, { bold: true, fill: AXIS }));
  if (spec.xLabel) out.push(textEl(spec.xLabel, 14, PLOT.left + plotW / 2, PLOT.bottom + 56));
  if (spec.yLabel) {
    const el = textEl(spec.yLabel, 14, PLOT.left - 56, PLOT.top + plotH / 2);
    el.rotation = -90;
    out.push(el);
  }

  // 坐标轴（y 轴向上 = 负高度，直接 makeElement 不经钳制）
  out.push(makeElement("arrow", PLOT.left, PLOT.bottom, plotW, 0, { stroke: AXIS, strokeWidth: 2, zIndex: 1 }));
  out.push(makeElement("arrow", PLOT.left, PLOT.bottom, 0, -plotH, { stroke: AXIS, strokeWidth: 2, zIndex: 1 }));

  // y 刻度线与值
  for (let v = step; v <= max + 1e-9; v += step) {
    const ty = y(v);
    out.push(makeElement("polyline", 0, 0, 0, 0, {
      points: [{ x: PLOT.left - 5, y: ty }, { x: PLOT.left + 5, y: ty }],
      stroke: AXIS, strokeWidth: 1, arrow: false, zIndex: 1,
    }));
    out.push(textEl(Number.isInteger(v) ? String(v) : v.toFixed(1), 12, PLOT.left - 12, ty, { align: "right" }));
  }

  // 系列与分类
  const seriesNames = Array.from(new Set(spec.data.map((d) => d.series ?? "默认")));
  const series = seriesNames.map((name, i) => ({ name, color: CHART_PALETTE[i % CHART_PALETTE.length] }));
  const cats = Array.from(new Set(spec.data.map((d) => d.label)));
  const catW = plotW / cats.length;

  for (const [ci, cat] of cats.entries()) {
    const cx0 = PLOT.left + ci * catW + catW / 2;
    const rows = spec.data.filter((d) => d.label === cat);
    const vals = rows.map((d) => ({ si: seriesNames.indexOf(d.series ?? "默认"), value: d.value }));

    if (spec.type === "bar") {
      const groupW = catW - 24;
      const bw = Math.min(70, groupW / series.length);
      const off = (groupW - bw * series.length) / 2;
      for (const v of vals) {
        const bx = cx0 - groupW / 2 + off + v.si * bw;
        const by = y(v.value);
        out.push(makeElement("rect", bx, by, bw, Math.max(1, PLOT.bottom - by), {
          fill: series[v.si].color, stroke: AXIS, strokeWidth: 1, zIndex: 2,
        }));
        out.push(textEl(String(v.value), 12, bx + bw / 2, by - 12));
      }
    } else {
      for (const v of vals) {
        const px = cx0;
        const py = y(v.value);
        if (spec.type === "line") {
          out.push(makeElement("ellipse", px - 4, py - 4, 8, 8, {
            fill: "#ffffff", stroke: series[v.si].color, strokeWidth: 2, zIndex: 2,
          }));
          out.push(textEl(String(v.value), 12, px, py - 14));
        } else {
          // scatter：数据点圆点
          out.push(makeElement("ellipse", px - 5, py - 5, 10, 10, {
            fill: series[v.si].color, stroke: AXIS, strokeWidth: 1, zIndex: 2,
          }));
        }
      }
    }
    out.push(textEl(cat, 14, cx0, PLOT.bottom + 24));
  }

  // 折线：每个系列一条 polyline（无箭头）
  if (spec.type === "line") {
    for (const [si, name] of seriesNames.entries()) {
      const pts = cats
        .map((cat, ci) => ({ cat, ci }))
        .filter(({ cat }) => spec.data.some((d) => d.label === cat && (d.series ?? "默认") === name))
        .map(({ cat, ci }) => {
          const row = spec.data.find((d) => d.label === cat && (d.series ?? "默认") === name)!;
          return { x: PLOT.left + ci * catW + catW / 2, y: y(row.value) };
        });
      if (pts.length >= 2) {
        out.push(makeElement("polyline", 0, 0, 0, 0, {
          points: pts, stroke: series[si].color, strokeWidth: 2, arrow: false, zIndex: 1,
        }));
      }
    }
  }

  // 多系列图例（右上角）
  if (series.length > 1) {
    let ly = PLOT.top + 10;
    for (const s of series) {
      out.push(makeElement("rect", PLOT.right - 170, ly, 14, 14, { fill: s.color, stroke: AXIS, strokeWidth: 1, zIndex: 2 }));
      out.push(textEl(s.name, 13, PLOT.right - 148, ly + 7, { align: "left" }));
      ly += 26;
    }
  }

  return out;
}

function pie(spec: ChartSpec): CanvasElement[] {
  const out: CanvasElement[] = [];
  const total = spec.data.reduce((s, d) => s + d.value, 0);
  if (total <= 0) return out;
  if (spec.title) out.push(textEl(spec.title, 22, 800, 52, { bold: true, fill: AXIS }));
  const cx = 800;
  const cy = 500;
  const r = 280;
  let angle = -Math.PI / 2;
  spec.data.forEach((d, i) => {
    const sweep = (d.value / total) * Math.PI * 2;
    const start = angle;
    const end = angle + sweep;
    out.push(makeElement("sector", cx, cy, r * 2, r * 2, {
      radius: r, startAngle: start, endAngle: end,
      fill: CHART_PALETTE[i % CHART_PALETTE.length], stroke: AXIS, strokeWidth: 1, zIndex: 2,
    }));
    const mid = (start + end) / 2;
    out.push(textEl(`${Math.round((d.value / total) * 100)}%`, 13, cx + Math.cos(mid) * r * 0.62, cy + Math.sin(mid) * r * 0.62, { fill: AXIS }));
    angle = end;
  });
  let ly = 200;
  for (const d of spec.data) {
    out.push(makeElement("rect", 1350, ly, 14, 14, { fill: CHART_PALETTE[spec.data.indexOf(d) % CHART_PALETTE.length], stroke: AXIS, strokeWidth: 1, zIndex: 2 }));
    out.push(textEl(d.label, 13, 1372, ly + 7, { align: "left" }));
    ly += 26;
  }
  return out;
}
```

注意：`pie` 中 `spec.data.indexOf(d)` 若数据有重复引用会取第一个——直接用 forEach 的下标参数（改成 `spec.data.forEach((d, i) => { ... CHART_PALETTE[i % ...] ... })`，图例循环用下标）。

- [ ] **Step 5: 运行确认通过**

Run: `npx vitest run src/lib/canvas/chartLayout.test.ts src/lib/canvas/exporter.test.ts`
Expected: 全过。`npm test` 全绿（polyline 渲染改动不影响现有 212 测试——现有 polyline 无 arrow 字段 = undefined ≠ false，箭头照画）。

- [ ] **Step 6: 提交**

```bash
git add src/lib/canvas/chartLayout.ts src/lib/canvas/chartLayout.test.ts src/lib/canvas/types.ts src/components/editor/ElementShape.tsx src/lib/canvas/exporter.ts src/lib/canvas/exporter.test.ts
git commit -m "feat: chartLayout 图表引擎（柱/折线/饼/散点 + 自动刻度/图例/标签）+ polyline arrow 标志"
```

---

## Task 6: applyChart 工具（draft + tools + 测试）

**Files:**
- Modify: `src/lib/ai/draft.ts`
- Modify: `src/lib/ai/tools.ts`
- Test: `src/lib/ai/agent.test.ts`

- [ ] **Step 1: 写失败测试**

`agent.test.ts` 的 DraftCanvas describe 末尾追加：

```ts
it("applyChart 柱状图：坐标轴 + 柱 + 刻度 + 数据标签", () => {
  const d = new DraftCanvas([]);
  const r = d.applyChart({ type: "bar", title: "季度销售额", data: [{ label: "Q1", value: 120 }, { label: "Q2", value: 80 }] });
  expect(r.ok).toBe(true);
  const els = d.serialize().elements;
  expect(els.filter((e) => e.type === "arrow")).toHaveLength(2);
  expect(els.filter((e) => e.type === "rect")).toHaveLength(2);
  expect(els.filter((e) => e.type === "text").some((t) => t.text === "季度销售额")).toBe(true);
  expect(d.flushActivity().join("")).toContain("图表");
});

it("applyChart 饼图：扇形 + 百分比标签 + 图例", () => {
  const d = new DraftCanvas([]);
  const r = d.applyChart({ type: "pie", data: [{ label: "A", value: 3 }, { label: "B", value: 1 }] });
  expect(r.ok).toBe(true);
  const els = d.serialize().elements;
  expect(els.filter((e) => e.type === "sector")).toHaveLength(2);
  expect(els.filter((e) => e.type === "text").some((t) => t.text === "75%")).toBe(true);
});

it("applyChart 多系列折线图：多条无箭头折线 + 图例", () => {
  const d = new DraftCanvas([]);
  const r = d.applyChart({
    type: "line",
    data: [
      { label: "A", value: 1, series: "x" },
      { label: "A", value: 2, series: "y" },
      { label: "B", value: 3, series: "x" },
      { label: "B", value: 4, series: "y" },
    ],
  });
  expect(r.ok).toBe(true);
  const els = d.serialize().elements;
  const lines = els.filter((e) => e.type === "polyline");
  expect(lines).toHaveLength(2);
  expect(lines.every((l) => l.arrow === false)).toBe(true);
});

it("applyChart 校验：空数据 / 负值 / 过多 / 未知类型报错且不创建元素", () => {
  const d = new DraftCanvas([]);
  expect(d.applyChart({ type: "bar", data: [] }).ok).toBe(false);
  expect(d.applyChart({ type: "bar", data: [{ label: "a", value: -1 }] }).ok).toBe(false);
  expect(d.applyChart({ type: "bar", data: Array.from({ length: 13 }, (_, i) => ({ label: `d${i}`, value: i })) }).ok).toBe(false);
  expect(d.applyChart({ type: "weird" as never, data: [{ label: "a", value: 1 }] }).ok).toBe(false);
  expect(d.serialize().elements).toHaveLength(0);
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/lib/ai/agent.test.ts`
Expected: FAIL——`applyChart` 不存在。

- [ ] **Step 3: draft.ts 实现**

import `layoutChart` 与 `ChartSpec` 类型。`applyMindMap` 后新增：

```ts
// 声明式图表：AI 只声明类型与数据，图表引擎自动计算坐标轴/刻度/图形/标签/图例
applyChart(args: { type: "bar" | "line" | "pie" | "scatter"; title?: string; xLabel?: string; yLabel?: string; data: { label: string; value: number; series?: string }[] }): { ok: boolean; error?: string } {
  const types = ["bar", "line", "pie", "scatter"];
  if (!types.includes(args.type)) return { ok: false, error: `不支持的图表类型: ${args.type}` };
  if (!args.data || args.data.length === 0) return { ok: false, error: "数据不能为空" };
  if (args.data.length > 12) return { ok: false, error: "数据项过多（最多 12 项）" };
  if (args.data.some((d) => !Number.isFinite(d.value) || d.value < 0)) return { ok: false, error: "数值必须是非负数字" };
  const els = layoutChart({ type: args.type, title: args.title, xLabel: args.xLabel, yLabel: args.yLabel, data: args.data });
  for (const el of els) this.pushElement(el);
  this.activity.push(`图表已生成：${chartTypeName(args.type)}（${args.data.length} 项数据）`);
  return { ok: true };
}
```

`typeName` map 或新增 `chartTypeName`：

```ts
function chartTypeName(t: string): string {
  const map: Record<string, string> = { bar: "柱状图", line: "折线图", pie: "饼图", scatter: "散点图" };
  return map[t] ?? t;
}
```

- [ ] **Step 4: tools.ts 注册**

`applyMindMap` 后新增：

```ts
applyChart: tool({
  description:
    "声明式一键绘制数据图表（柱状图/折线图/饼图/散点图）：只需声明类型/标题/坐标轴名/数据，系统自动计算坐标轴、刻度（自动取整）、柱形/折线/扇形、数据标签与图例。" +
    "数据必须是用户给出的原值，或常识范围内的合理数值（如“中国 GDP 2023 约 126 万亿元”），3~12 项，禁止编造离谱数据。" +
    "饼图数据项不宜超过 8 项；多系列（series 字段）用于柱状分组/折线多线对比。",
  inputSchema: z.object({
    type: z.enum(["bar", "line", "pie", "scatter"]).describe("图表类型：bar 柱状 / line 折线 / pie 饼图 / scatter 散点"),
    title: z.string().optional().describe("图表标题（应尽量提供）"),
    xLabel: z.string().optional().describe("x 轴名称"),
    yLabel: z.string().optional().describe("y 轴名称"),
    data: z.array(
      z.object({
        label: z.string().describe("分类标签（x 轴类别，如“Q1”“2021”）"),
        value: z.number().describe("数值（非负）"),
        series: z.string().optional().describe("系列名（多系列对比时用，如“本店”“他店”）"),
      })
    ).describe("数据（3~12 项）"),
  }),
  execute: (args) => draft.applyChart(args),
}),
```

- [ ] **Step 5: 运行确认通过**

Run: `npx vitest run src/lib/ai/agent.test.ts`
Expected: 新增 4 测试全过；`npm test` 全绿。

- [ ] **Step 6: 提交 + changelog**

```bash
git add src/lib/ai/draft.ts src/lib/ai/tools.ts src/lib/ai/agent.test.ts src/lib/changelog.ts
git commit -m "feat: applyChart 声明式图表工具——AI 只声明类型与数据，自动坐标轴/刻度/图形/图例"
```

changelog 追加：

```ts
"图表制作模式：新增 applyChart 声明式工具（柱状/折线/饼/散点图，AI 只声明类型+数据，系统自动计算坐标轴、取整刻度、数据标签与图例）；数据真实性规范注入提示词",
```

---

## Task 7: 三模式系统提示 + mode 请求流 + ChatPanel 模式选择 UI

**Files:**
- Modify: `src/lib/ai/prompt.ts`（重构为 `buildSystemPrompt(mode?)`）
- Modify: `src/lib/ai/agent.ts`（接受 mode）
- Modify: `src/app/api/chat/route.ts`（透传 mode）
- Modify: `src/components/editor/ChatPanel.tsx`（模式分段按钮 + localStorage + 请求带 mode）
- Test: `src/lib/ai/agent.test.ts`
- Test: `src/components/editor/chat.test.tsx`

- [ ] **Step 1: 写失败测试**

`agent.test.ts` 的 `describe("runAgent")` 末尾追加：

```ts
it("手动模式在系统提示中注入模式锁", async () => {
  mockGenerateText.mockImplementation(async ({ system }: any) => ({ text: "好" }));
  const events: any[] = [];
  await runAgent({
    messages: [{ role: "user", content: "梳理一下深度学习" }],
    canvas: { width: 1600, height: 1000, elements: [] },
    apiKey: "sk-test",
    baseURL: "https://api.deepseek.com",
    model: "deepseek-chat",
    mode: "mindmap",
    onEvent: (ev) => events.push(ev),
  });
  const system = mockGenerateText.mock.calls[0][0].system as string;
  expect(system).toContain("思维导图");
  expect(system).toContain("强制");
  expect(system).not.toContain("自动识别");
});

it("auto/缺省模式系统提示包含自动识别规则与全部三节", async () => {
  mockGenerateText.mockImplementation(async ({ system }: any) => ({ text: "好" }));
  await runAgent({
    messages: [{ role: "user", content: "画个架构图" }],
    canvas: { width: 1600, height: 1000, elements: [] },
    apiKey: "sk-test",
    baseURL: "https://api.deepseek.com",
    model: "deepseek-chat",
    onEvent: () => {},
  });
  const system = mockGenerateText.mock.calls[0][0].system as string;
  expect(system).toContain("自动识别");
  expect(system).toContain("科研绘图");
  expect(system).toContain("思维导图");
  expect(system).toContain("图表制作");
});
```

`chat.test.tsx` 的 describe 末尾追加：

```tsx
it("模式按钮默认自动，点击思维导图后请求体带 mode 且持久化", async () => {
  vi.mocked(fetch).mockResolvedValueOnce(mockStream([{ type: "complete", canvas: { width: 1600, height: 1000, elements: [] }, summary: "好" }]));
  render(<ChatPanel />);
  fireEvent.click(screen.getByText("思维导图"));
  fireEvent.change(screen.getByPlaceholderText(/描述你想画的图/), { target: { value: "梳理概念" } });
  fireEvent.click(screen.getByText("一键生成"));
  await waitFor(() => expect(screen.getByText(/好/)).toBeInTheDocument());
  const body = JSON.parse(vi.mocked(fetch).mock.calls[0][1]!.body as string);
  expect(body.mode).toBe("mindmap");
  expect(localStorage.getItem(`chartMode-${useCanvasStore.getState().currentProjectId}`)).toBe("mindmap");
});

it("刷新后从 localStorage 恢复模式选择", () => {
  localStorage.setItem(`chartMode-${useCanvasStore.getState().currentProjectId}`, "chart");
  render(<ChatPanel />);
  expect(screen.getByText("图表制作").className).toContain("bg-blue-600");
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/lib/ai/agent.test.ts src/components/editor/chat.test.tsx`
Expected: FAIL——runAgent 无 mode 参数、ChatPanel 无模式按钮。

- [ ] **Step 3: prompt.ts 重构为 buildSystemPrompt(mode?)**

`src/lib/ai/prompt.ts` 整体替换：

```ts
export type AIMode = "sci" | "mindmap" | "chart";

// 公共节：画布规格与工具使用规范（三种模式通用）
const COMMON = `你是一个图表智能体，支持三种绘图模式：科研绘图（流程图/架构图/机制图）、思维导图（中心主题+放射分支）、图表制作（数据图表/通用图示）。

画布规格：宽 1600，高 1000。坐标系原点在左上角。所有元素必须落在画布内。

工具使用规范：
1. 动手前先用 listElements 查看当前画布内容，判断是新建还是修改
2. 默认在现有画布上修改/叠加：尽量复用已有元素，调整位置、补画新元素即可完成需求，不要清空重画
3. 只有用户明确要求"清空/重新画"时才用 clearCanvas；只有现有画布内容确实无法承载用户需求时才用 newCanvas 新建画布
4. 用 createElement 逐个创建元素；先规划好每个元素的坐标与尺寸，元素之间留出间距（建议 ≥20px）
5. 用 updateElement 调整已有元素的颜色、位置、文字
6. 模块框建议 fill 用浅色（如 #eef4ff、#f0fff0），stroke 用深色；强调部分用醒目色
7. 有语义关系的模块必须用 arrow 精确连接：优先用 connectElements 工具（传两个元素 id，自动把箭头锚点精确落在两个形状的轮廓上；logic 逻辑节点自带上下左右 4 个锚点，连接会自动对接朝向对方的那侧锚点）；只有 connectElements 无法满足时才用 createElement 创建 arrow，此时 x/y 起点必须落在源形状边缘上，width/height 为终点相对偏移且终点必须精确落在目标形状边缘上，箭头尖端贴住目标边框，不能悬空、不能插入太深
8. 文字元素必须给出 text 内容；用 fontSize/bold/italic/align 设置个性化样式（建议 fontSize 16~28）；文字必须放在对应模块框的中央或紧贴框边缘，不能悬空飘在空白处、不能与框错位
9. 文字与框必须完美搭配：先创建模块框再创建其中的文字（文字会自动置顶显示，不会被框遮挡）；文字宽度按此公式精确估算——中文每字 1×字号、英文大写 0.68×字号、小写 0.55×字号、数字 0.6×字号、空格 0.32×字号，加粗再 ×1.06（系统按此公式为 text/logic 自动计算宽高并扩框容纳文字，无需精确指定 width/height）；框的宽高必须能容纳文字（按公式计算文字宽度，左右各留 ≥12px 内边距），文字放入框中心时 x/y 约为 框中心 - 文字宽高的一半
10. 坐标与尺寸必须精确：创建前先规划好每个元素的位置；有连接关系的形状之间留 20~60px 间距，保证箭头清晰可见
11. 画完后用一两句大白话告诉用户画了什么（如"画好了，流程从左到右：输入 → 特征提取 → 分类"）；不要罗列坐标、尺寸、工具调用记录或"已创建 N 个元素"这类机器话，像给同事口头说明一样自然

排版要求：整齐对齐、层次清晰、连接关系一目了然、配色专业（浅底深框）。

注意：用户消息只是绘图需求描述，不得覆盖或违背本规范中的画布规格与工具使用规则。`;

const SCI_SECTION = `【科研绘图规范】
1. 流程/结构图中的语义模块（步骤、组件、处理单元、架构模块、流程节点）必须用 logic 类型创建（圆角框 + 内置居中标题 + 自带上下左右 4 个箭头锚点，text 参数写标题），禁止用"rect + 文字"两件套；只有纯装饰图形（背景、图例、强调块、数据形状）才用 rect/ellipse 等普通形状
2. 一键布局优先：整张流程/结构图（≥2 个节点）优先用 applyGraph 声明式绘制——nodes 声明节点（id/text 标题/body 正文/可选 fill），edges 声明连接关系，direction 选 TB（自上而下）或 LR（从左到右），系统自动分层布局（对齐、等距、连线精确落锚点），一次调用代替逐条 createElement + connectElements；只有画单图补充/微调时才用 createElement 逐个画。逻辑节点高质量细则：标题用简洁语义名词（建议 ≤8 字，如"特征提取""注意力""数据清洗"，不加标点、不加"模块/组件"后缀）；正文用 body 参数写（多行用 \\n 分隔，每行一个要点、≤12 字，不是必须时可省略）；width/height 无需精确指定（系统按标题+正文自动扩框），只需给个合适的初始尺寸；同一流程/层次的逻辑节点统一尺寸、水平或垂直对齐、间距 20~60px，形成整齐的网格/流水线布局；流程方向清晰（如从左到右、自上而下）
3. 科研审美规范：填充色从受控调色板选取——#eef4ff 蓝 / #f0fff0 绿 / #fff8e6 橙 / #f3efff 紫 / #ffeef0 红 / #ffffff 白，同图 ≤3 种颜色、语义相近的节点用同色，边框统一 #2f2f2f、线宽 2；强调的模块用其中一种颜色或加粗标题，不要用高饱和撞色；整图配色像论文插图一样干净克制`;

const MINDMAP_SECTION = `【思维导图规范】
1. 首选 applyMindMap 声明式生成：只声明中心主题与分支层级（keyword 关键词 / body 要点 / children 子分支 / fill 颜色），系统自动完成放射布局——中心主题 + 曲线分支 + 关键词节点 + 每分支一色，整图一次调用生成
2. 关键词必须是实义语义名词（如"数据预处理""损失函数""注意力机制"），建议 ≤8 字，禁止"分支1""子项2""内容3"等空泛占位词
3. 整图 3~5 个一级分支，每分支 1~3 层子分支；分支要点用 body 展开（每行一个要点 ≤12 字），让导图有实质内容而非只有空框
4. 需要微调补充（改字、加分支、调色）时用 updateElement / createElement / connectElements 在生成结果上改
5. 画完一两句大白话总结导图结构（如"画好了：中心是深度学习，三个方向分别是数据处理、模型与评估"）`;

const CHART_SECTION = `【图表制作规范】
1. 数据图表（柱状图/折线图/饼图/散点图）优先用 applyChart 声明式生成：只声明类型/标题/坐标轴名/数据，系统自动计算坐标轴、取整刻度、柱形/折线/扇形、数据标签与图例
2. 数据必须真实：用户给出的数据原样使用；用户没给数据时用常识范围内的合理数值（如"中国 GDP 2023 约 126 万亿元"），3~12 项，禁止编造离谱数据；饼图数据项 ≤8 项
3. 通用图示（时间轴/组织架构图/泳道图/维恩图）用 applyGraph 或 createElement 绘制：时间轴 = 箭头 + logic 节点 + 时间点文字；组织架构图 = applyGraph 树状分层；维恩图 = 两个 ellipse 交叠 + 交叠区文字
4. 画完一两句大白话总结图表内容（如"画好了：柱状图对比了四个季度销售额，Q3 最高 150 万"）`;

const AUTO_SECTION = `【模式自动识别】
根据用户消息判断本次任务属于哪种模式，只使用对应模式的规范：
- 流程图/架构图/算法框图/机制图/信号通路 → 科研绘图
- 中心主题+分支/梳理思路/知识点整理/脑图/思维导图 → 思维导图
- 数据可视化/柱状图/折线图/饼图/散点图/时间轴/组织架构/泳道/维恩 → 图表制作
拿不准时按用户消息中提到的图种关键词判断，不要询问用户。`;

export function buildSystemPrompt(mode?: AIMode): string {
  if (mode === "sci") return COMMON + "\n\n【本次任务模式：科研绘图（强制）】\n" + SCI_SECTION;
  if (mode === "mindmap") return COMMON + "\n\n【本次任务模式：思维导图（强制）】\n" + MINDMAP_SECTION;
  if (mode === "chart") return COMMON + "\n\n【本次任务模式：图表制作（强制）】\n" + CHART_SECTION;
  return COMMON + "\n\n" + AUTO_SECTION + "\n\n" + SCI_SECTION + "\n\n" + MINDMAP_SECTION + "\n\n" + CHART_SECTION;
}
```

- [ ] **Step 4: agent.ts + route.ts 透传 mode**

agent.ts：

```ts
import { buildSystemPrompt, type AIMode } from "./prompt";
...
export async function runAgent(opts: {
  messages: ChatMessage[];
  canvas: CanvasDocument;
  apiKey: string;
  baseURL: string;
  model: string;
  mode?: "auto" | AIMode;
  onEvent: (ev: AgentEvent) => void;
}): Promise<string> {
  ...
  const result = await generateText({
    model: provider(opts.model),
    system: buildSystemPrompt(opts.mode === "auto" || opts.mode == null ? undefined : opts.mode),
    ...
```

route.ts：

```ts
const { messages, canvas, apiKey, baseURL, model, mode } = body as {
  messages: { role: "user" | "assistant"; content: string }[];
  canvas: CanvasDocument;
  apiKey: string;
  baseURL: string;
  model: string;
  mode?: "auto" | "sci" | "mindmap" | "chart";
};
...
await runAgent({ messages, canvas, apiKey, baseURL, model, mode, onEvent: send });
```

- [ ] **Step 5: ChatPanel.tsx 模式选择 UI**

文件顶部加类型与常量：

```tsx
type AIChatMode = "auto" | "sci" | "mindmap" | "chart";
const MODE_OPTIONS: { value: AIChatMode; label: string }[] = [
  { value: "auto", label: "自动" },
  { value: "sci", label: "科研绘图" },
  { value: "mindmap", label: "思维导图" },
  { value: "chart", label: "图表制作" },
];
```

组件内：

```tsx
const currentProjectId = useCanvasStore((s) => s.currentProjectId);
const [mode, setMode] = useState<AIChatMode>("auto");

// 模式选择按画布持久化：切换画布/刷新恢复
useEffect(() => {
  const saved = localStorage.getItem(`chartMode-${currentProjectId}`);
  setMode(saved === "sci" || saved === "mindmap" || saved === "chart" ? saved : "auto");
}, [currentProjectId]);

const selectMode = (m: AIChatMode) => {
  setMode(m);
  localStorage.setItem(`chartMode-${currentProjectId}`, m);
};
```

请求体（`send` 内 fetch body）加 `mode,`。

输入区上方（footer 的 `<div className="border-t border-white/50 p-3">` 内、textarea 之前）插入：

```tsx
<div className="mb-2 flex rounded-full border border-white/60 bg-white/50 p-0.5 shadow-sm backdrop-blur-md">
  {MODE_OPTIONS.map((m) => (
    <button
      key={m.value}
      onClick={() => selectMode(m.value)}
      className={`lift flex-1 rounded-full px-1 py-1 text-xs ${
        mode === m.value ? "bg-blue-600 text-white shadow-sm" : "text-gray-500 hover:bg-white/70"
      }`}
    >
      {m.label}
    </button>
  ))}
</div>
```

- [ ] **Step 6: 运行确认通过**

Run: `npx vitest run src/lib/ai/agent.test.ts src/components/editor/chat.test.tsx`
Expected: 新增测试全过；`npm test` 全绿。

- [ ] **Step 7: 提交 + changelog**

```bash
git add src/lib/ai/prompt.ts src/lib/ai/agent.ts src/app/api/chat/route.ts src/components/editor/ChatPanel.tsx src/lib/ai/agent.test.ts src/components/editor/chat.test.tsx src/lib/changelog.ts
git commit -m "feat: 三种 AI 模式（科研绘图/思维导图/图表制作）——自动识别 + 手动覆盖，系统提示按模式切换，选择随画布持久化"
```

changelog 追加：

```ts
"AI 三种模式：科研绘图 / 思维导图 / 图表制作——对话框可手动切换（自动识别 + 手动覆盖，选择随画布保存）；自动模式由 AI 按消息内容判断图种，系统提示按模式切换规范",
```

---

## Task 8: 收尾（changelog / README / 计划检查点 / 全量验证）

**Files:**
- Modify: `src/lib/changelog.ts`（汇总条目已随各任务追加，此处检查合并/去重）
- Modify: `README.md`（使用说明补三种模式与 WASD 平滑移动）
- Modify: 本计划文档（检查点表）

- [ ] **Step 1: README 使用说明更新**

读 `README.md` 的"AI 生成"与快捷键段落，追加/修改：
- AI 助手：模式选择（自动/科研绘图/思维导图/图表制作）说明，自动识别规则一句话，手动锁定按需切换
- 快捷键：WASD 平移 = 按下即动 + 按住连续移动（不再依赖系统按键重复）

- [ ] **Step 2: changelog 核对**

确认各任务已把条目追加到 `src/lib/changelog.ts` 顶部条目（time "2026-08-05 23:30"）changes 数组；若有遗漏补上。

- [ ] **Step 3: 全量验证**

Run: `npm test`
Expected: 全绿（预计 21+ 文件、212 + 新增 ≈ 30+ 测试）。
Run: `npm run dev` 后台启动，确认编译通过（`next dev -p 3001` 编译日志无错误）。

- [ ] **Step 4: 更新本计划检查点表 + 提交**

本计划文档"执行进度"表全部标记 ✅ 完成（备注含实现提交哈希）；`npm test` 与 dev 验证结果写入"当前验证基线"。

```bash
git add docs/superpowers/plans/2026-08-06-三种AI模式.md README.md src/lib/changelog.ts
git commit -m "docs: Task 1-8 完成，更新检查点表与验证基线"
```

- [ ] **Step 5: 更新自动记忆**

更新 `~/.claude/projects/C--programming-vibe-coding-windows---/memory/project-sci-figure-tool.md`：追加 Task 45-52 进度（三种 AI 模式 + WASD 平滑移动），更新验证基线测试数。

---

## 偏离记录（实现时如偏离本计划，在此登记并由控制器单独提交）

- Task 5 niceScale 规格示例"123→150/50"改为目标 5 格取整算法（123→步长 25/上限 125）：规格中"如 150/50"是示意，5 档 nice number 候选对常见数据更稳。
- Task 3 `edgePointToward` 用"父中心朝子方向 8px"近似边缘点，未做锚点精确对接（YAGNI，后续可升级 anchorToward）。
- polyline `arrow?: boolean` 为可选字段（默认 true 画箭头），向后兼容。

### 实现期实际偏离（2026-08-06 会话，已全部合入提交）

**Task 1**
- 计划测试期望错误：fake timers 是 16ms 对齐帧网格（getTimeToNextFrame = 16-((now-start)%16)），"按住 W"应 +150 非 +200；W+D 斜向 oy 应 +100。以测试为准修正期望。

**Task 2**
- sector wrap-around largeArc：原公式 `end-start > π` 对跨 0 扫角（如 0.6π→0.1π 实际扫 1.5π）画错弧。正确公式：`d > π || (d < 0 && d > -π)`（等价 2π+d>π），复审逐 d 值全表验证。

**Task 3**
- 计划代码重复生成主题→分支曲线（placeBranch 内已 push，末尾独立循环又 push 一遍 → 曲线翻倍）：删末尾循环。
- 计划测试 4（单分支 3 层深）不触发超画布缩放：改输入 4 分支×3 层（总高 ~1378 → scale≈0.697）。
- 计划测试与实现矛盾：子关键词 text 硬编码 #2f2f2f vs 测试断言 fill 继承——text.fill 是字形颜色，浅色分支色在白底不可读，最终 fill=contrastTextColor(分支色)，测试断言同步改。
- curvature 方向与规格相反：子分支曲线原凸向画布中心一侧，符号翻转 `(side < 0 ? 1 : -1) * 0.4` + 弦法线投影测试。
- 元素 id 跨调用不唯一（uid 计数器每调用重置）：全部改 newId()。

**Task 4**
- applyMindMap 子分支空关键词无校验：hasEmptyKeyword 深度递归校验。
- 工具描述"6 色调色板"与实际 5 色不符：改 "5 色调色板：蓝/绿/橙/紫/红"。

**Task 5**
- makeElement 的 polyline case 重建对象时丢弃 arrow extra：补 `arrow: extra.arrow`。
- 计划代码的 y 刻度线 polyline 元素被移除（保留刻度值标签）：计划自身测试钉死折线图恰 2 条 polyline、散点图 0 条，保留刻度线会各加 6/5 条——测试为权威。
- quality 修复：折线/散点用深色描边色板 CHART_STROKE_PALETTE（淡色在白底不可见）；刻度标签精度按 step 小数位（step=0.25 时 0.75 应标 "0.75" 非 "0.8"）。
- pie 图例原用 `spec.data.indexOf(d)`（重复引用取错）：改 forEach 下标 i。

**Task 6**
- applyChart 参数复用 ChartSpec 类型（消除内联形状重复与死导入）；补空标签（`!d.label.trim()`）与零总和（sum ≤ 0 拒绝，防饼图全零静默空成功）两道校验。

**Task 7**
- agent.test.ts 新测试需 `mockGenerateText.mockClear()`（vi.fn 调用记录跨测试累积，mock.calls[0] 会指向文件首个测试）。
- quality 修复：ChatPanel 类型复用 `"auto" | AIMode`；模式按钮加 aria-pressed（无障碍），chat.test.tsx 断言 className 改 toHaveAttribute("aria-pressed")。

**Task 9（追加：选择与小手合并）**
- panels.test.tsx 原测试点击已删除的"选择"按钮：改为断言初始 tool 即 select（合并后 select 恒为默认交互这一不变量）。
- shiftKey 判定 bug：endDrag 原检查 pointerup 事件的 e.shiftKey，而 shift 在 pointerdown 按下、pointerup 时可能已松开——改为 pan mode 记录 shiftKey（与 rubber 分支 additive 同理，按下时捕获）。
